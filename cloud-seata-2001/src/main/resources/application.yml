
# TODO　seata当时学的时候主要是看阳哥的视频　自己没有手敲　下载的Seata版本比阳哥高太多 于是没有搭建环境

server:
  port: 2001
spring:
  application:
    name: seata2001
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    alibaba:
      seata:
        tx-service-group: default

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://8.136.104.77:3306/seata
    username: root
    password:
